services:
  - type: web
    name: curebot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web:server --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.2
